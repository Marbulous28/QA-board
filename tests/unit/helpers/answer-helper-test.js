import { answerHelper } from 'qa-board/helpers/answer-helper';
import { module, test } from 'qunit';

module('Unit | Helper | answer helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerHelper([42]);
  assert.ok(result);
});
